<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tqitreineeflowbank">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ACTIVITY.GerarEmpActivity"></activity>
        <activity android:name=".ACTIVITY.EmprestimosActivity" />
        <activity
            android:name=".ACTIVITY.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ACTIVITY.TermosActivity" />
        <activity android:name=".ACTIVITY.InicioActivity" />
        <activity android:name=".ACTIVITY.CadastroActivity" />
        <activity android:name=".ACTIVITY.LoginActivity" />
        <activity android:name=".ACTIVITY.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>